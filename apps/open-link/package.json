{
	"name": "@superset/open-link",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev --port ${WRANGLER_PORT:-8790}",
		"deploy": "wrangler deploy",
		"typecheck": "bunx tsc -p tsconfig.json --noEmit --emitDeclarationOnly false"
	},
	"devDependencies": {
		"typescript": "^5.9.3",
		"wrangler": "^4.14.4"
	}
}
