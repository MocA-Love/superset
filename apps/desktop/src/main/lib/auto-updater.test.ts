import { describe, test } from "bun:test";

describe("installUpdate", () => {
	test.todo("upstream quitAndInstall duplicate-click guard is disabled in this fork release-page updater", () => {});
	test.todo("fork updater should cover release-page open and status reset if automatic install is restored", () => {});
});
