import { describe, test } from "bun:test";

describe("workspaceCreation create/checkout input validation", () => {
	test.todo(
		"workspaceCreation.create was removed from this fork; keep create validation on workspaces.create",
	);
	test.todo(
		"workspaceCreation.checkout was removed from this fork; keep PR checkout validation on workspaces.create",
	);
	test.todo(
		"PROJECT_NOT_SETUP coverage now belongs to workspaces.create / workspaceCreation.adopt paths",
	);
});
